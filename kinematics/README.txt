This script makes the monte carlo vs data comparison plots.
Also the reco vs truth plots
Run it with: python3 reco_true_check.py -i -b
if you do not care about nuGen comparisons and want to use
the best fit values for the Atmospheric weights from the config file
